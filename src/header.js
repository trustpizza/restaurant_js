import Background from "";

const header = () => {
    const header = document.querySelector("header");

    const container = document.createElement("div");
    container.classList.add("text-center", "bg-gray-50", "text-gray-800", "py-20", "px-6", "bg-top");
    
};

export { header }; 